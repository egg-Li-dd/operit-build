# 边界、验证与 P1 移交 [DONE]

旧实现：

- 无调用链表，无观测出口，无脱敏工具，无树形还原。
- P0 启动前仅 `multimodel_candidate_pool_20260916` 遗留（候选池 + 选路核），本阶段在其上增加旁路观测。

意图修正：

- 明确列出“P0 不做”的红线，防止实现阶段被顺手塞进降级/降级链/查询接口。
- 建立验收基准，把每项需求映射到代码位置与测试用例。
- 列出 P1 需要承接的 5 项遗留，避免下一期重复踩坑。

新实现：

验证矩阵：

| 场景 | 输入 | 期望 |
| --- | --- | --- |
| P0 落地完整性 | 无新宿主进程 | 两张新表、迁移 v20→v21、5 条索引 |
| 选路决策可见 | 三策略 × 空池 / 全禁用 / 单候选 / 多候选 | trace.strategy / reason 正确；空池 candidate=null、pickedIndex=-1 |
| 主模型落点可见 | FIXED 同 pool 连发 N 次 | 同一 instance；ROUND_ROBIN 依次；WEIGHTED 符合比例 |
| 观测失败不阻塞 | 模拟 sink 抛异常 | AppLogger.w 记录，请求正常返回 |
| 脱敏覆盖三类凭据 | Bearer / api_key=xxx / sk-xxx | 原始串消失，scheme/key 名/前缀保留 |
| 误伤零容忍 | task=done / risk-averse / desk-lamp / 裸 Bearer | 原文原样 |
| 树构建不丢 span | 父缺失 / 自引用 / 两节点纯环 / 带入口环 | spanCount 完整；500 层深不爆栈 |
| DDL 与实体一致 | 改实体或改 DDL | 守卫测试立刻失败 |

红线（P0 不做）：

- 不写降级 / 弱模型兜底 —— P1 做。P0 只预留 `WEAK` / `DEGRADED` 取值与 `degraded` 字段。
- 不写子模型池 —— P1 做。P0 只预留 `SUB` tier。
- 不写查询接口与信息界面 —— P2 做。`getRecentTraces` / `getSpansForTrace` / `deleteTracesBefore` 等方法已备好 DAO，调用方在 P2 接。
- 不在采集点散着脱敏 —— 只在出口边界做一次（见 04）。
- 不猜调用成败 —— P0 的 span 语义是“选路决策 + 租约存活期”，成功/失败回填留给 P1 业务层。
- 不改选路语义、不改缓存键、不改工厂分派、不改限流维度。

P1 移交（必须在开始 P1 前处理，否则验收不闭合）：

| 遗留 | 影响 | 建议 |
| --- | --- | --- |
| `chatId` 恒 null | 按会话聚合 trace 的 UI 暂时看不到分组 | P1 透传 `chatId` 进 `acquireServiceForFunction`（改动 1 处 + 加测试）→ 已完成，见 06 |
| trace 保留策略未定 | 长时间运行会无限增长 | 加 `AiCallTraceRetentionScheduler` 每日清理（见 05 决策矩阵，建议 7d / 30d / 90d）→ 已完成，见 06 |
| 观测顺序非严格 | 并发写库 `RUNNING` 覆盖终态（已观测到一次真实日志） | P1 若需要严格序：单写者队列（已知边界，见 03） |
| P1 降级链路回填 | P0 的 span.status 永远 SUCCESS | P1 把真实失败类型 / token 数 / 成本写入对应 span → 已完成（status / tokens / cost 回填 + 首次失败根因），见 07 |
| 无查询入口 | P0 落库了但查不到 | P2 接 MCP 工具 + JsTools 包装 + 信息界面 |

待确认（决策矩阵）：

| 议题 | 方案 A（草案） | 方案 B | 影响 |
| --- | --- | --- | --- |
| trace 默认保留天数 | 7 天：兼顾磁盘压力与问题回溯 | 30 天：长期排障方便 | 影响 05 保留调度器参数 |
| 降级标记 | P0 预留 `degraded` 字段 + `AiCallSpanStatus.DEGRADED` | 留等 P1 定义触发条件 | 现有代码不改语义，零成本 |

推荐：

- 保留天数先给 7 天。用户真机磁盘压力是真实风险，30 天可在用户反馈后切。
- 降级标记先占位；定义条件留给 P1。

i18n：

- P0 无新增用户可见字符串（`reason` 仅落库，不展示）。若 P1/P2 需展示决策原因，统一走已有 `SystemToolPromptsInternal.kt` 扩展。

完成口径：

- 本次代码改动 11 文件（新增 10 + 修改 1）+ 4 个测试文件（27 例）+ 5 份文档。按仓库 TODO 规范，`[DONE]` 已在 02/03/04/05 一级标题末尾标注；01 为现状分析参考，无代码工作单元，不单独标注。全部变更已落盘，按仓库执行准则未运行构建与测试。